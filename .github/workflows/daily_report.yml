name: Daily Automation Master

on:
  schedule:
    - cron: '0 21 * * *'  # 매일 오전 6시 KST (UTC 21시) — 지연 감안하여 여유 확보
  workflow_dispatch:  # 수동 실행 버튼

jobs:
  run-all-tasks:
    runs-on: ubuntu-latest

    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
      NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
      NAVER_CLIENT_ID_PACSAFE: ${{ secrets.NAVER_CLIENT_ID_PACSAFE }}
      NAVER_CLIENT_SECRET_PACSAFE: ${{ secrets.NAVER_CLIENT_SECRET_PACSAFE }}

    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🐍 Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 의존성 설치
        run: pip install requests anthropic bcrypt

      - name: 📊 프레지던트 이메일 리포트
        run: python president_report.py
        continue-on-error: true

      - name: 📊 팩세이프 이메일 리포트
        run: python pacsafe_report.py
        continue-on-error: true

      - name: ✅ 작업 완료
        run: echo "🎉 이메일 리포트 발송 완료!"
